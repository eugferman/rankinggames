import React from "react";
import {useSelector} from 'react-redux';

function SortGamesScore() {

  const gamesRedux = useSelector(state => state.games);

  
  return <div>
    </div>
}

export default SortGamesScore;